# downstream/

This directory is reserved for **Phase 5 — Downstream + audit timeline**.

It will hold the downstream storage layer (persistent store of valid records)
and the audit timeline that records every scrape run, validation result, and
heal decision, so the system's self-healing behavior is observable over time.
